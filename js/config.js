/* ============================================================
   SITE CONFIG
   ------------------------------------------------------------
   CONTACT FORM SETUP (one step, ~60 seconds):

   1. Go to  https://web3forms.com
   2. Enter  rishrag18@gmail.com  and press "Create Access Key".
   3. Check that inbox for the access key (a UUID).
   4. Paste it below, replacing REPLACE_WITH_YOUR_WEB3FORMS_ACCESS_KEY.

   The Web3Forms access key is designed to be public — it only lets a
   browser post a message to YOUR verified inbox. It is not a secret
   API key and exposing it in frontend code is the documented usage.
   No server, no backend, works on GitHub Pages / Netlify / Vercel.

   Until the key is set, the Send Message button falls back to opening
   the visitor's mail client with the message pre-filled, so it is
   never a dead button.
   ============================================================ */
window.SITE_CONFIG = {
  WEB3FORMS_ACCESS_KEY: "REPLACE_WITH_YOUR_WEB3FORMS_ACCESS_KEY",
  OWNER_EMAIL: "rishrag18@gmail.com",
  FORM_SUBJECT_PREFIX: "Portfolio enquiry",
  FROM_NAME: "Portfolio — rishiragavendra"
};
